import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Agentforce Cost Shock Calculator | Gated Enterprise",
  description: "Discover the real cost of Salesforce Agentforce after hidden fees, implementation, and org configuration. Get your personalized ROI report instantly.",
  openGraph: {
    title: "Agentforce Cost Shock Calculator",
    description: "What does Agentforce really cost? Find out in 60 seconds.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
